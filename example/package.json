{
  "name": "int2words-example",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host --open",
    "build:docs": "vite build --emptyOutDir",
    "preview": "vite preview --open",
    "lint": "npm run lint:script && npm run lint:style",
    "lint:script": "eslint",
    "lint:style": "stylelint '**/*.scss'"
  },
  "dependencies": {
    "int2words": "file:../"
  },
  "devDependencies": {
    "globals": "^16.0.0",
    "postcss": "^8.5.3",
    "postcss-scss": "^4.0.9",
    "sass": "^1.85.1",
    "stylelint": "^16.15.0",
    "stylelint-config-standard-scss": "^14.0.0",
    "vite": "^6.2.0"
  }
}
